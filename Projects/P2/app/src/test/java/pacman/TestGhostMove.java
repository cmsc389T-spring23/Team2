package pacman;

import java.io.*;
import junit.framework.*;
import java.awt.Color;

public class TestGhostMove extends TestCase {

  public void testGhostMove() throws FileNotFoundException {
    // Ghost ghost = new Ghost(new Location(1, 1), new Color(0, 0, 0), "ghost", "ghost");
    // ghost.move();
    // assertEquals(ghost.getLoc().x, 1);
    // assertEquals(ghost.getLoc().y, 1);
    // ghost.setLoc(new Location(1, 1));
    // ghost.setDir(0);
    // ghost.move();
    // assertEquals(ghost.getLoc().x, 1);
    // assertEquals(ghost.getLoc().y, 0);
    // ghost.setLoc(new Location(1, 1));
    // ghost.setDir(1);
    // ghost.move();
    // assertEquals(ghost.getLoc().x, 2);
  }
}
