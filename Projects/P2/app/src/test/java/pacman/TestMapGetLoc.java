package pacman;

import java.io.*;
import junit.framework.*;
import java.awt.Color;
import pacman.Map.Type;

public class TestMapGetLoc extends TestCase {

  public void testMapGetLoc() throws FileNotFoundException {
    // Map map = new Map("map1.txt");
    // Location loc = new Location(1, 1);
    // Location loc2 = new Location(1, 2);
    // Location loc3 = new Location(1, 3);
    // Location loc4 = new Location(1, 4);

    // HashSet<Type> types = map.getLoc(loc);
    // HashSet<Type> types2 = map.getLoc(loc2);
    // HashSet<Type> types3 = map.getLoc(loc3);
    // HashSet<Type> types4 = map.getLoc(loc4);

    // assertTrue(types.contains(Type.EMPTY));
    // assertTrue(types2.contains(Type.WALL));
    // assertTrue(types3.contains(Type.COOKIE));
    // assertTrue(types4.contains(Type.EMPTY));
  }
}
